# NextVWT PTT App Prototype

This is a code bundle for NextVWT PTT App Prototype. The original project is available at https://www.figma.com/design/cJZKy4tSRAnv7p2Fzv8XbE/NextVWT-PTT-App-Prototype.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
